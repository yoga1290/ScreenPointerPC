It's just virtual HTTP server that sends screen-shots of your other machine over the same network through the Web Browser,and control your mouse curser.

It uses Google jQuery/AJAX to handle the requests!